name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install machin
        run: |
          curl -fsSL https://raw.githubusercontent.com/javimosch/machin/main/install.sh | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Verify machin
        run: machin guide | head -c 60

      - name: Check
        run: make check

      - name: Test
        run: make test

      - name: Build
        run: make build

      - name: Smoke test
        run: |
          DB=$(mktemp -d)
          ID=$(./grange put --db "$DB" --doc '{"v":1}' | python3 -c 'import sys,json;print(json.load(sys.stdin)["data"]["id"])')
          ./grange get --db "$DB" --id "$ID"
          ./grange count --db "$DB"
          rm -rf "$DB"
